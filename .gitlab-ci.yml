pages:
  script:
    - mkdir .public
    - cp -r * .public
    - cp max_speed.html .public/index.html
    - mv .public public
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
